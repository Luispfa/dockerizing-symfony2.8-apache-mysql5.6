{% verbatim %}
    <div class="page" ng-controller="personalAVsCtrl">

        <section class="panel panel-default">
            <div class="panel-heading"><strong><span class="glyphicon glyphicon-th"></span> <span i18n="id00440_app:av:own-questions">Ruegos y Preguntas Propios</span></strong></div>
            <div class="panel-body">


                <div class="table-responsive table-bordered">
                    <table class="table table-striped table-bordered">
                        <thead>
                            <tr>
                                <th class="col-sm-10" i18n="id00441_app:av:description">Descripción proceso</th>
                                <th class="col-sm-2"  i18n="id00441_app:av:date">Fecha</th>
                            </tr>
                        </thead>
                        <tbody>
                            <tr ng-if="items.length === 0">
                                <td i18n="id00092_app:no-data">No hay datos disponibles</td>
                                <td></td>
                            </tr>
                            <tr ng-repeat="item in items">
                                <td>
                                    <h4><a ng-click="goToQuestion(item)">{{item.subject | entitiesDecode}} - {{i18n(states[item.state])}}<span ng-if="item.locked == 1"> ({{i18n(states['locked'])}})</span></a></h4>
                                    <p ng-bind-html="item.body | nl2br"></p>
                                </td>
                                <td>{{item.date_time | date: 'dd/MM/yyyy&nbsp;HH:mm'}}</td>
                            </tr>
                        </tbody>
                    </table>  
                </div>


            </div>
        </section>


    </div>


{% endverbatim %}